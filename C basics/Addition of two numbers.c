#include <stdio.h>
// Addition of Two Numbers

int main()
{
    int a, b, c; // declaring 3 vars
    printf("Enter Two Numbers\n");
    scanf("%d%d", &a, &b);
    c = a + b; // this will add two numbers
    printf(" %d + %d = %d hoi", a, b, c);
    return 0;
}
